Implementar o normalize css do github para normalizar para todos os navegadores

Corrigir responsividade do site usando a forma de medida rem no css;

Modificar font e tamanho da font de todas as páginas; x 

Colocar os links certos de redirecionamento das páginas;

Fazer html das páginas: 
    - Dúvidas;
    - Configurações;
    - Homepage do site sobre quem nós somos e o que é o Drawback;

Fazer js das páginas: 

    - index.html;
        > função de se vincular a uma empresa;
        >> função de adicionar documentos;
        >>> função de atualizar documentos;
        >>>> função de atualizar a validade dos documentos;
        >>>>> fazer com que a cada documento adicionado e a cada campo de documento, apareça na barra e relatório de progresso

    - loginUser:
        > conectar com banco de dados;
        >> checar dados no banco de dados;
        >>> criar uma sessão para esse usuário.

    - registerUser:
        > conectar com banco de dados;
        >> persistir informações no bando de dados;
        >>> mandar usuário para fazer login.

    - loginCompany:
        > conectar com banco de dados;
        >> checar dados no banco de dados;
        >>> criar uma sessão para esse usuário.

    - registerCompany:
        > conectar com banco de dados;
        >> persistir informações no bando de dados;
        >>> mandar usuário para fazer login.